// packages/domain/tsconfig.json
{
  "extends": "@word-bomb/typescript-config/node-lib.json",
  "compilerOptions": {
    // If the library truly runs in both Node and the browser, leave DOM here.
    // Otherwise remove "DOM" to avoid accidental usage in Node-only consumers.
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"]
}
